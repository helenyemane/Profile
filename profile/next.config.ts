import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
   images: {
    domains: [
      'tesfa-53b1c4b2cf65.herokuapp.com',
      // add other domains if needed
    ],
  },
  // ...any other config options
};


export default nextConfig;
